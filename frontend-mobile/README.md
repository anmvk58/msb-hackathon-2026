# Frontend Mobile

Customer-facing Mobile Banking demo served by Nginx. It connects to Mock Core
Banking for login, accounts, transactions, budgets and goals, and to Financial
Radar for the recommendation, selection and confirmation workflow.

Open `http://localhost:3000` after running `docker compose up -d --build` from
the repository root.
