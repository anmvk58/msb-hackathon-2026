# Copy to mobile.env on the Mobile server. Never commit mobile.env.
# Listen on all IPv4 interfaces so http://SERVER_PUBLIC_IP works on port 80.
MOBILE_BIND_IP=0.0.0.0
MOBILE_CORE_ORIGIN=http://10.0.0.10:8090
MOBILE_AGENT_ORIGIN=http://10.0.0.30:8080
